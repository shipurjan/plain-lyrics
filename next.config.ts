import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  output: 'export',
  allowedDevOrigins: ['giggling-ornament-costing-liberty-mythical.eu-1.24h.sh'],
}

export default nextConfig
